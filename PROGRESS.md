# 照见千年 · 开发进度与决策记录

> 铜镜 × 朝代审美变化的小红书小工具。SLC 方案见 [SLC.md](./SLC.md)。
> 本文档记录**已确定的决策、调研结论与风险清单**。与 SLC.md 冲突之处，以本文档为准。

---

## 一、当前状态

- [x] SLC 方案定稿（SLC.md）
- [x] 关键决策确认（见第二节）
- [x] 小红书小工具约束调研（见第三节）
- [x] 技术方案设计（见第四节）
- [x] 初始化项目骨架（Vite + React + TS）✅ M1
- [x] 占位图脚本 ✅ M1（`npm run placeholders`，8 张 webp，单张 16–30KB）
- [x] 核心交互开发（滑动切换 / 点击翻面）✅ M2（浏览器实测通过，见 M2 记录）
- [x] 热点与史实资料 ✅ M3（浏览器实测通过，见 M3 记录）
- [x] 视觉打磨 ✅ M4（霞鹜文楷子集接入 + 展柜光池，见 M4 记录）
- [ ] **素材升级（M4.5，进行中）**：管线与工具就绪，等美术出图
  - ✅ `npm run assets:optimize`：原始大图（`assets-src/mirrors/<id>/{front,back}.png`）→ 缩放 1200²、圆形抠图转透明底（`--mask-r` 可调半径 / `--no-mask`）、webp 压缩、超预算告警；背景图（`assets-src/backgrounds/`）→ 1080×2340 cover。假图全流程实测通过（含四角透明/中心不透明验证）
  - ✅ 热点标定工具：`http://localhost:6180/?calibrate`，点图取百分比坐标、载入现有热点、删除、一键复制 JSON 回填 `mirrors.ts`
  - ✅ **POC → 主页面接线完成**：`Mirror3D` 受控组件（翻面由手势层驱动；不传 flipped 则点击自翻供 POC 用）；主页面 3D 优先、WebGL 不可用自动回退 CSS 平面翻面；热点层上提至舞台层（3D/CSS 共用，3D 正视时百分比坐标对齐）；四面镜子全部接入（圆/八角/葵口轮廓，葵口相位已对齐）；正面材质调暗为磨光青铜。全链路实测：渲染/翻面/滑动切换/热点淡入与过滤均通过。**遗留调优**：正面质感（磨光黑漆古方向）、WebGL 真机验证（R3）
  - ⏳ 镜面 8 图：方向=**写实 PBR + 代码光照**（用户确认），唐/宋/明已出图接入，汉图待按新规范重出
  - ⏳ 「金过头」调优：真实页面语境下评估 → 分区粗糙度贴图（凸亮凹哑）待做
  - ⏳ 朝代背景：图层机制未动工（等背景素材方向明确）
- [ ] 视觉走查与验收（对照 SLC.md 第九节完成标准）
- [ ] 小红书打包适配（推后至素材定稿后）

**当前阶段：素材升级（M4.5）。打包（原 M5）按用户决定推后至素材定稿。**

### M4 完成记录（2026-09-01）

- **霞鹜文楷接入**：v1.522 Regular + Medium（家族无 Bold，标题字重由 Medium 映射 700）；`scripts/subset-font.mjs` 扫描 src/ 与 index.html 全部字符（606 字，含 ASCII/中文标点兜底）子集化，**24.4MB → 126KB/字重**；产物在 `src/fonts/*.woff2`（Vite 处理 hash 与相对路径，比 publicDir + 绝对路径更稳）；新增文案后重跑 `npm run font:subset`；完整 TTF 不进版本库（.gitignore）；
- **展柜光池**：镜子后方极淡的射灯径向光晕 + 四角暗角（`.app::before`），强化「线上小展览」氛围且不违背「避免大面积花纹」；
- 修复：信息卡与热点圆点的 CSS transform 会被 Framer Motion 动画整体覆盖的居中问题（位移统一交给 Framer 的 x/y 合成）；
- 桌面端 / 移动端截图走查通过；生产构建：JS gzip 87.4KB + 字体 2×129KB。

### M3 完成记录（2026-09-01）

### M2 完成记录（2026-09-01）

- 上下滑动切换（循环）、点击翻面、滚轮/方向键/侧边按钮全部实测通过（390×844 移动端 + 1280×800 桌面端）；
- 拖拽不会误触翻面（tap 与 drag 由 Framer Motion 手势系统区分）；
- **踩坑记录**：`onTap` 不能挂在 drag 元素的子元素上（draggable 父元素会抑制子级 tap 手势），必须挂在 drag 元素自身，翻面状态相应上提到 MirrorStage；
- 切换阈值：位移 70px 或速度 350px/s；键盘/按钮冷却 650ms，滚轮冷却 900ms（吸收触控板连续滚动）；
- 方向语义：上滑/滚轮下/ArrowDown = 下一个朝代（阅读流语义，三者一致）；
- dev 端口改为 **6180**（5173/5273 附近被其他项目占用），preview 端口 6181，均加 `strictPort`；
- 生产构建：JS gzip 85.9KB。

### M3 完成记录（2026-09-01）

- **热点交互**：进入镜背停留 1.6 秒后热点错峰淡入（很淡的呼吸动画，不闪烁）；翻面/换镜后重置重计；点击热点打开底部信息卡，点空白/关闭按钮返回；热点点击不会误触翻面（`closest('.hotspot')` 原生 DOM 过滤——React 合成层的 stopPropagation 拦不住 drag 元素的原生监听，这也是一个踩坑点）；
- **史实资料预留（reference 字段）**：数据 schema 新增可选 `reference`（title/detail/imageUrl?/source?/sourceUrl?），imageUrl 缺省时 UI 显示「史料图片整理中 · 待补充」占位块，后续补充图片只需填字段；
- **馆藏实拍图**：汉、唐两面镜子已配克利夫兰艺术博物馆开放授权（CC0）实拍图（汉·四神博局式青铜镜 1995.301 / 唐·瑞兽葡萄镜 1995.356），下载至 `assets/references/` 并压为 webp（107KB / 45KB），信息卡内标注来源与授权；宋（湖州石家镜）、明（五子登科镜）未找到合适的开放授权馆藏图，**暂为占位块**，可后续自行补图；
- 底部新增低调的「史实资料」入口按钮；卡片打开时切换朝代会自动关闭；
- 馆藏图在小红书沙箱中同样离线可用（本地打包，无外链）；`sourceUrl` 链接仅网站版可点；
- 生产构建：JS gzip 87.3KB。

---

## 二、已确认的决策

| # | 决策项 | 结论 | 备注 |
|---|--------|------|------|
| D1 | 技术栈 | Vite + React + TypeScript + Framer Motion | 理由见第四节 |
| D2 | 翻面交互 | 点击铜镜本体，沿 Y 轴翻转正反面 | 按 SLC 第三节执行 |
| D3 | 朝代切换 | **上下滑动**（原 SLC 为左右滑动，已变更） | 汉→唐→宋→明，自上而下 |
| D4 | 占位图 | 由脚本生成一套统一规格占位图 | 不阻塞交互开发，正式素材到位后只换图 |
| D5 | 字体 | [霞鹜文楷 LxgwWenkai](https://github.com/lxgw/LxgwWenkai) | **必须子集化后打包进代码包**，见 D7 |
| D6 | 目标平台形态 | 小红书 **Builder Hub「小工具」**（非小程序） | 调研结论见第三节 |
| D7 | 资源策略 | 全部资源本地打包，**零网络请求** | 小红书沙箱硬约束，见第三节 |
| D8 | 滑动边界行为 | 循环切换（上滑=下一个，下滑=上一个；明上滑绕回汉，汉下滑绕到明） | 已实测通过 |
| D9 | **美术立体感方案** | **代码 3D（three.js）**：生图只出「平涂正视纹样图（+灰度凹凸图）」，立体感由实时渲染承担 | 2026-09-02 定。背景：AI 直接画立体光影必翻车（伪光影光路不统一），写实又打不过馆藏实拍；立体感外包给实时渲染是两个弱项的规避。管线：平涂图 → 高度图 → 法线贴图 → three.js 圆盘 PBR + 环境光照。交互白赚：翻面变真 3D 旋转、光线可随触摸流动。成本：three 约 170KB gzip（预算内）；沙箱 WebGL 需真机验证（并入 R3）；WebGL 不可用回退 CSS 3D 平面方案。先做 POC（拿已生成的 `sishou.png` 验证），立得住再全面切换 |

### D3 补充说明（上下滑动带来的连锁调整）

SLC 第七节「滑动手势不能与页面纵向滚动冲突」原本是针对左右滑动设计的。改为上下滑动后：

- 页面采用**全屏固定布局，本身不发生纵向滚动**，纵向手势完全让给朝代切换；
- 桌面端对应支持：鼠标滚轮、上下箭头按钮、键盘方向键；
- 原方案中「铜镜从左/右滑入滑出」的过渡动画改为**纵向滑入滑出 + 淡出**，其余不变。

---

## 三、小红书「小工具」调研结论（2026-09）

### 3.1 平台形态辨析

小红书有两条容易混淆的路，本项目走 **Builder Hub 小工具**：

| | Builder Hub「小工具」 | 开放平台「小程序」 |
|---|---|---|
| 入口 | 创作者中心 Builder Hub，填名称/简介/封面，上传 zip | 开放平台注册开发者资质，走审核 |
| 技术形态 | **标准 Web 技术（HTML/CSS/JS）**，静态产物 | 类小程序双线程框架（xhs.* API） |
| 门槛 | 零门槛，无需开发者账号 | 需企业/个人主体资质，审核较严 |
| 挂载 | 直接挂载到笔记 | 挂载到笔记/直播间等 |
| 参考 | [人人都是产品经理实测](https://www.woshipm.com/ai/6432615.html)、[智源社区报道](https://hub.baai.ac.cn/view/56595) | [开放平台文档](https://miniapp.xiaohongshu.com) |

### 3.2 沙箱硬约束（影响架构的三条）

来自[第三方整理](https://xmsumi.com/detail/3827)：

1. **只支持本地运行，不支持任何网络请求** —— 图片、字体、全部 JS/CSS 必须打进代码包；不能引用任何 CDN、外链字体、统计脚本；
2. **每个小工具独立隔离，运行在安全沙箱** —— 默认按 iframe 沙箱预期行为处理触摸事件与 viewport；
3. **上传 zip，且需符合平台规范格式，否则部署直接失败** —— 具体规范（目录结构、入口文件名）需在实际上传时对照官方文档，见风险清单。

### 3.3 对本项目的直接影响

- **字体**：霞鹜文楷完整版 TTF 体积巨大（十余 MB），必须做**子集化**。本项目总文案量很小（标题 + 4 段简介 + 热点卡片 ≈ 数百汉字），用 `pyftsubset`（fonttools）按实际字符生成 woff2，预期可压到 **几十~几百 KB**。子集脚本纳入构建流程，文案变更后重跑即可；
- **图片**：正式铜镜素材需压缩（webp，单张控制在几百 KB 内），占位图脚本直接生成轻量格式；
- **构建产物**：Vite 需配置 `base: './'`（相对路径），保证 zip 包内离线可用；
- **零依赖外部请求**：不引入任何 analytics / 图标 CDN / 在线字体，全部资源本地化；
- **代码包体积预算**：平台确切上限未查到官方数字（见风险清单），自设保守预算 **≤ 5MB**，实际目标 2–3MB。

### 3.4 后期扩展为普通网站的兼容性设计

小红书版与普通网站版**共用同一套源码**，仅在构建产物层面分流：

- 内容（文案、热点坐标、图片路径）全部在数据配置文件中，与平台无关；
- 业务组件不感知宿主环境，平台相关逻辑（如有）收敛到独立的 adapter 文件；
- `npm run build` 产出标准静态站（可直接部署 Vercel / 任意静态托管）；
- `npm run build:xhs` 产出符合小红书规范的 zip 包；
- 普通网站版将来可自由追加：CDN 字体、分享功能、外链、统计分析——代码上只需放开 adapter，不动业务层。

---

## 四、技术方案

### 4.1 技术栈（决策 D1）

**Vite + React 18 + TypeScript + Framer Motion**，不使用 UI 框架，样式用原生 CSS（CSS 变量管理主题色）。

选型理由：

- 小红书小工具 = 标准 Web 沙箱，Vite 静态产物直接可用；
- Framer Motion 对本项目三类核心动画（纵向滑动切换、Y 轴 3D 翻面、信息卡进出场）都是声明式配置，且自带手势系统（`drag` + 方向锁定），tap 与滑动的判定阈值开箱即用；
- 体积可控：React + Framer Motion gzip 后约 150KB，相对图片和字体资产可忽略；
- TypeScript 保证镜面数据配置的结构安全（SLC 第四节 JSON 结构定义 schema）。

### 4.2 目录规划

```text
mirror-museum/
  src/
    data/            # 铜镜数据配置（SLC 第四节 schema，含 TS 类型）
      mirrors.ts     # 4 个朝代的内容配置，热点坐标用百分比
    components/      # 展示组件（不硬编码任何铜镜内容）
      MirrorStage    # 主舞台：滑动切换 + 翻面
      MirrorCard     # 单面铜镜 + 热点
      Hotspot        # 热点圆点 + 呼吸动画
      InfoCard       # 热点信息卡
      DynastyNav     # 朝代指示与切换按钮
    adapters/        # 平台差异收敛处（小红书 / 普通网站）
  scripts/
    make-placeholders.mjs   # 占位图生成脚本（决策 D4）
    subset-font.mjs         # 霞鹜文楷子集化脚本（决策 D5）
  assets/
    mirrors/{han,tang,song,ming}/{front,back}.webp   # SLC 第五节结构
  fonts/           # 子集化后的 woff2
```

### 4.3 占位图方案（决策 D4）

脚本（Node + Canvas）生成 8 张统一规格图：正方形画布、透明背景、居中圆形镜体剪影；背面带简单纹样区分环带，正面留「镜面」高光渐变；每朝代用不同的暗色调以便开发时肉眼区分。文件名与 SLC 第五节目录约定完全一致，正式素材直接覆盖同名文件。

### 4.4 霞鹜文楷接入（决策 D5，M4 已落地）

- 从 [lxgw/LxgwWenkai](https://github.com/lxgw/LxgwWenkai) Releases 获取 TTF（v1.522 Regular + Medium）；
- `scripts/subset-font.mjs`（npm `subset-font`，harfbuzzjs，纯 JS 无需 Python）扫描 `src/` + `index.html` 全部字符子集化，输出 `src/fonts/*.woff2`（Vite 管理路径与 hash）；
- 实测：606 字符，24.4MB → 126KB/字重；文案变更后重跑 `npm run font:subset`；
- 完整 TTF 放项目根目录作子集化输入，不进版本库。

---

## 五、风险与待确认清单

| # | 风险/待确认 | 影响 | 应对 |
|---|------------|------|------|
| R1 | Builder Hub 代码包**确切大小上限**未找到官方数字 | 若预算不足需砍素材体积 | 自设 ≤5MB 保守预算；实际上传时对照官方文档 |
| R2 | zip 的**规范格式细节**（入口文件、目录结构）不明，第三方实测称不规范会部署失败 | 打包脚本可能需要调整 | 首次上传前查阅 Builder Hub 官方开发文档；打包脚本做成可配置 |
| R3 | 沙箱内触摸手势行为（iframe 中纵向 swipe 是否被宿主页拦截） | 核心交互 | 开发时用 touch-action + preventDefault 双保险；真机小红书内实测 |
| R4 | 审核要求：第三方称「太基础的 demo 不给过」 | 上架 | 本项目有完整视觉与内容，风险低；保留此提醒 |
| R5 | 热点坐标依赖正式素材构图 | 换图后热点错位 | 坐标存配置文件，交接素材时一并标定微调 |

---

## 六、里程碑

1. **M1 骨架可用**：项目初始化、占位图生成、4 面镜子数据配置，静态展示单面镜子；
2. **M2 核心交互**（对应 SLC 优先级第 1 条）：上下滑动切换朝代（含循环）、点击翻面、tap/滑动阈值打磨；
3. **M3 热点与文案**：热点出现逻辑（停留 1–2 秒淡入）、信息卡完整进出体验、四段简介文案（突出宋/明与其他两朝的审美反差）；
4. **M4 视觉打磨**：配色、字体、过渡动效细节，对照 SLC 第六节；
5. **M5 小红书打包**：字体子集化、zip 打包脚本、真机实测；
6. **M6 验收**：对照 SLC 第九节完成标准逐条走查。

---

## 八、素材规格（M4.5，2026-09 定）

### 8.1 铜镜主体（真实素材）

| 项 | 规格 |
|---|---|
| 画布 | **1200 × 1200 px 正方形**（显示最大 520 CSS px，3x DPR 富余） |
| 背景 | **透明**（webp alpha / PNG 交付后转 webp） |
| 主体占比 | 镜体外径约占画布 **86%**（半径 ≈ 516px），居中、不裁切 |
| 视角 | 正视（镜面正对观者），四面镜子统一 |
| 光照 | 统一左上主光、右下弱补光，禁止纯正面平光 |
| 格式/体积 | webp（alpha），单张 **≤ 300KB**，8 张共 ≤ 2MB |
| 命名 | 沿用 `assets/mirrors/<id>/{front,back}.webp`，直接覆盖 |

生成方式建议：**AI 生图为主**（即梦/Midjourney 等），按朝代出正反两面；纹样形制的史实准确性不苛求（史实锚点由「史实资料卡」的馆藏实拍图承担，两者角色不同）。图先出 Back（纹样面），Front 可复用同一素面模板换色调。

交付流程：原始图（PNG，可超大）丢 `assets-src/mirrors/<id>/` → 跑 `npm run assets:optimize`（sharp 批量缩放至 1200、圆形抠图转透明、转 webp、压体积）→ 自动归位到 `assets/mirrors/` → 在 `http://localhost:6180/?calibrate` 标定热点 → 回填 `mirrors.ts`（R5）。

### 8.2 朝代背景

| 项 | 规格 |
|---|---|
| 画布 | **1080 × 2340 px**（移动端全屏 cover，桌面端裁切可接受） |
| 内容 | 暗色、低对比（亮度波动 ≤ 20%）、无具象大花纹（SLC 第六节），纹样只作极淡暗纹出现 |
| 朝代基调 | 汉·漆黑陶红 + 云气暗纹 / 唐·金棕 + 团花暗纹 / 宋·青灰 + 山水淡影 / 明·朱褐 + 织金暗纹 |
| 格式/体积 | webp，单张 **≤ 250KB**，共 4 张 ≈ 1MB |

两档实现：**A. SVG 程序化纹理**（turbulence 噪声 + 渐变，近零体积，我可立即做）；**B. AI 生成实材质图**（质感更佳，走同样 optimize 管线）。建议先用 A 占位定氛围，B 出图后无缝替换。

### 8.3 体积预算复核

| 项 | 现状 | 预算 |
|---|---|---|
| JS | 87KB gzip | — |
| 字体 | 2 × 129KB | — |
| 镜面 8 张 | 143KB（占位） | ≤ 2MB |
| 背景 4 张 | 0（渐变） | ≤ 1MB |
| 馆藏参考图 | 155KB | — |
| **合计** | ≈ 0.6MB | **≤ 4MB**，仍远低于自设 5MB 上限 |

---

## 七、参考链接

- [霞鹜文楷 GitHub](https://github.com/lxgw/LxgwWenkai)
- [小红书开放平台（小程序，本项目不采用）](https://miniapp.xiaohongshu.com)
- [Builder Hub 报道：小红书笔记长出 App](https://hub.baai.ac.cn/view/56595)
- [小工具上传实测（人人都是产品经理）](https://www.woshipm.com/ai/6432615.html)
- [小工具沙箱约束整理](https://xmsumi.com/detail/3827)
- [小红书小组件（开放平台）踩坑记](https://juejin.cn/post/7564540677478301759)

# 交接文档（2026-09-09 当前权威入口）

项目：**照见千年**，两页序厅 + 九朝铜镜（商、春秋、战国、汉、隋、唐、宋、元、明）的互动展览。技术栈为 Vite / React 18 / TypeScript / Framer Motion / Three.js，目标同时覆盖手机浏览器、GitHub Pages 和小红书小工具环境。

> 本文以当前工作区代码为准。2026-09-03 的性能报告、2026-09-05 路线图和 2026-09-08 UI 诊断保留为历史证据；若与本文冲突，以本文为准。

## 当前状态

2026-09-09 按“性能 → 视觉减法 → 镜面材质 → 响应式布局”的顺序完成的一轮修改已随 `d98654e` 发布。当前工作区在此基础上增加九朝“画卷唤醒 + 照见古人”，尚未提交或发布：

1. **性能和等待状态**：纹理改为“当前镜最高优先、下一镜优先、其余镜空闲预热”；翻页/过渡期间暂停后台上传。删除 `App.tsx` 中 800ms 强制进入尚未绘制画布的逻辑，加载失败会切换到已解码的 CSS 图片或稳定占位后再放行。
2. **视觉减法**：删除墙面 SVG 中与内容不对齐的金色线框、网点和大圆弧；史实资料入口改为安静的方章，取消涟漪；展厅内页码刻度和提示文字降噪。
3. **镜面正面**：以一次性生成的静态色彩图和粗糙度图代替纯色金属圆盘，增加打磨环纹、细划痕和边缘氧化；正面参数改为 `metalness: 0.46`、`roughness: 0.86`、`envMapIntensity: 0.13`，降低空白和过亮感。WebGL 回退也使用相同的哑光古铜语汇。
4. **布局和资料卡**：展厅改成稳定的展签 / 展品 / 控制三区。桌面端为侧置展签，手机端回到纵向三段，并为矮屏单独缩放。资料图放入暗色装裱框，标题与正文区域重新组织。
5. **画卷唤醒**：九个朝代各有横版与手机竖版平面画卷，中央铜镜先以压暗、低饱和的二维图像融入旧绢。第一次点击让同位置的 Three.js 铜镜渐显，第二次点击才翻面；切换朝代会回到画卷状态。画卷不采用房间、地面或透视场景，避免铜镜像悬在真实空间中。
6. **照见古人**：人物透明 WebP 现在作为镜面上的 Three.js 材质层，与镜面共享几何、翻转和倾斜，不再是独立 DOM 浮层。翻到镜面后自动显现，界面持续标注“人物意象 · 艺术复原”。商、春秋、战国刻意更抽象，汉至明逐渐接近绘画肖像；详细依据见 `docs/dynasty-reflection-sources-2026-09-09.md`。纹样热点改为点击“细看纹样”后显示，避免抢占铜镜的唤醒/翻面手势。

环境反射一直存在：`mirrorScene.ts` 使用 `RoomEnvironment` 经 PMREM 生成 `scene.environment`。旧 UI 诊断中“场景无环境贴图”的判断已被代码证伪；镜面空白的真实原因是正面材质没有表面纹理，且金属度和反射偏高。

## 查到的问题与证据

### 1. 刚打开时翻页比预热完成后慢

九面镜共有 18 张镜背颜色/法线纹理。旧策略在序厅期间依次准备全部资源，GPU 上传会和用户早期翻页争用主线程。桌面热切换测试在资源就绪后 8/8 完成且没有大于 50ms 的切换帧，但启动阶段曾测到单次纹理初始化约 30–45ms，并出现约 86ms 长任务。因此这轮修改针对的是**早期旅程的资源调度**，不是移除 3D。

诊断脚本新增 `?perf=early-swipe&dpr2`：打开后立即通过序厅并连续切镜，记录交互期间是否启动低优先级 GPU 上传。当前镜即使处于 waiting 仍允许准备；下一镜和其余镜只在无交互且页面可见时进入空闲调度。

### 2. 固定 800ms 放行可能产生空白

旧版在新镜未报告 `onReady` 时，800ms 后仍会进入。网络、解码或 GPU 上传超过该时间时，页面已经可见而 canvas 仍隐藏。现在只在新镜实际绘制完成，或 CSS 回退/错误占位完成布局后报告 ready；350ms 后显示“正在加载铜镜…”，但不再用时间替代真实就绪状态。

### 3. “画面怪”来自互相冲突的视觉层

序厅是古画和竖排题跋，旧展厅同时出现图纸式背景线、巨大朝代字、发光红按钮和完整 11 项刻度。墙面装饰又按固定 900×1600 坐标绘制，在不同视口裁切后像断线或渲染残留。删除这些装饰，并将展厅整理成三块稳定区域后，铜镜成为主视觉，辅助控件不再争抢注意力。

### 4. 正面空白不是资源失败

旧正面只有纯色 `MeshStandardMaterial`，没有 `map` 或 `roughnessMap`；已有环境反射会进一步放大“光滑金属片”的感觉。新正面纹理只在场景创建时生成和上传一次，不为九面镜分别下载资源，也不引入逐帧动画。

## 排查方法

1. 对照交接文档、当前代码和 Git 历史，先区分历史四镜修复与当前九镜问题。
2. 用自动脚本逐帧记录页面阶段、内容切换、渲染次数、纹理上传和长任务；分别测启动期与资源就绪后的切换。
3. 暂停或移除单一因素做对照：先前已证明可见瞬移来自翻页状态机；本轮又确认热切换正常，新增压力集中在九镜后台预热。
4. 在手机和桌面尺寸复拍序厅、镜背、镜面、热点卡和史实资料卡，再回到 CSS、SVG 和 Three.js 材质定位来源。
5. 为慢纹理和失败纹理保留可注入诊断入口，验证页面不会永久停在 waiting，也不会提前显示空 canvas。

## 不可破坏的性能约束

1. 内容可见时不得交换镜面素材；旧页完全淡出，新镜实际绘制后再进入。
2. 页面位移只有 `usePageNavigation` 一个写入者；停止的动画不能继续写状态。
3. renderer 常驻，不得按朝代 key 重建。纹理缓存必须缓存正在加载的 Promise，并与 renderer 同生命周期。
4. 静止时零持续绘制。只有翻面、鼠标倾斜、换素材和尺寸变化触发帧；触摸拖拽不驱动 3D 倾斜。
5. 用户交互期间不得启动下一镜或其余镜的后台 GPU 上传；当前 waiting 所需纹理例外。
6. `map` 与 `normalMap` 同时应用；仅在材质纹理槽位首次从空变为有图时更新 shader 特性。
7. StrictMode 在同一已连接 canvas 上清理再挂载时不得 `forceContextLoss`；真正卸载才释放上下文。
8. 资料卡打开时屏蔽背景翻页；`pointercancel` 不提交翻页；快速短滑保留速度判定。
9. 保持 `base: './'`，不增加 CDN、在线字体或运行时网络 API。
10. 人物旧影只在用户先唤醒铜镜、再主动翻到镜面后出现；不得在画卷初始态展示正视大脸。保持低头/侧视、头肩构图与“艺术复原”标注。

## 验证与限制

- 2026-09-09 当前工作区已通过 `npm run build`（TypeScript 检查与 Vite 生产构建）；构建仍有主包约 840 kB 的体积提示，属于后续拆包项，不影响本次构建成功。
- 自动交互：启动诊断服务后打开 `?interactions&dpr2`，再运行 `node scripts/diagnostics/check.mjs interactions`。
- 早期翻页：打开 `?perf=early-swipe&dpr2`，再运行 `node scripts/diagnostics/check.mjs early-swipe`。
- 桌面无头数据只能证明状态和任务调度，不能代表手机 GPU 帧率。
- 仍需真机重点检查：首次打开后立刻连续上滑、Firefox 地址栏收起/展开、矮屏文字和按钮是否完整、镜面正面的明暗与划痕是否自然、WebGL 失败后的 CSS 回退。

## 关键代码

- `src/App.tsx`：页面编排、waiting 放行、展厅三区和资料卡状态。
- `src/interaction/usePageNavigation.ts`：唯一翻页状态机。
- `src/components/MirrorStage.tsx`、`Mirror3D.tsx`、`MirrorFlip.tsx`：3D/CSS 回退与就绪契约。
- `src/rendering/mirrorScene.ts`：按需渲染、纹理优先级、空闲预热、环境反射和正面材质。
- `src/index.css`、`src/textures/wall-base.svg`：响应式展厅和视觉减法。
- `src/data/reflections.ts`、`src/textures/reflections/`：九朝人物旧影配置与本地透明素材。
- `src/data/tableaux.ts`、`src/textures/tableaux/`：九朝横版/手机版画卷配置与本地背景素材。
- `docs/dynasty-reflection-sources-2026-09-09.md`：每朝人物视觉依据、可信度与授权边界。
- `scripts/diagnostics/README.md`：诊断入口和判定命令。

## 后续建议

1. 先完成上述真机复核，再决定是否继续压缩贴图或降低手机 DPR；没有数据前保持 DPR 上限 2。
2. 若早期翻页仍慢，分别记录下载、图片解码和 GPU 上传耗时，不要直接恢复批量预热或删除 3D。
3. 九朝人物旧影已完成本地版本，发布前应真机逐朝检查清晰度与惊吓感；陀螺仪、拭镜和动态高光仍属后续功能，任何动态效果都必须先证明不会恢复空闲持续渲染。
4. `src/index.css` 仍较大，可在交互和视觉验收稳定后按序厅、展厅、资料卡拆分，避免一边重构一边调视觉。
5. 历史文档：`docs/zcode-handoff-2026-09-03.md` 记录四镜阶段性能修复，`docs/roadmap-2026-09-05.md` 记录功能设想，`docs/ui-diagnosis-2026-09-08.md` 记录本轮视觉问题的原始诊断。
